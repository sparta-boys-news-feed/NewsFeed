spring:
  application:
    name: NewsFeed

  datasource:
    url: jdbc:mysql://localhost:3306/newsfeed
    username: root
    password: "1234"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

